<UserControl
    x:Class="WordFormatterUI.Views.TemplateManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WordFormatterUI.ViewModels"
    xmlns:models="using:WordFormatterUI.Models.Templates"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:TemplateManagementViewModel}">

    <UserControl.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="TemplateListButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Content area -->
        <StackPanel Grid.Row="0" Spacing="24">
            <!-- Title -->
            <TextBlock
                Text="模板管理"
                Style="{StaticResource SectionTitleStyle}"
                Margin="0,0,0,0"/>

            <!-- Template list box -->
            <Border
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Background="{ThemeResource CardBackgroundFillColorDefault}"
                Height="320"
                Padding="0">
                <ScrollViewer
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl
                        ItemsSource="{x:Bind ViewModel.Templates, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:TemplateDto">
                                <Grid
                                    Height="40"
                                    Padding="12,0,8,0"
                                    VerticalAlignment="Center"
                                    Tapped="OnTemplateItemClick">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Template name -->
                                    <TextBlock
                                        Grid.Column="0"
                                        Text="{x:Bind Name}"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        TextTrimming="CharacterEllipsis"/>

                                    <!-- Delete button (hidden for default templates) -->
                                    <Button
                                        Grid.Column="1"
                                        Style="{StaticResource TemplateListButtonStyle}"
                                        Content="🗑"
                                        ToolTipService.ToolTip="删除模板"
                                        Visibility="{x:Bind IsDefault, Converter={StaticResource InvertBoolToVisibilityConverter}}"
                                        Click="OnDeleteClick"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- Fixed bottom bar: Import / Export buttons -->
        <Border
            Grid.Row="1"
            Padding="0,8,0,0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Style="{StaticResource ActionButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Content="导入模板"
                    Command="{x:Bind ViewModel.ImportTemplateCommand, Mode=OneWay}"/>

                <Button
                    Grid.Column="1"
                    Style="{StaticResource ActionButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Content="导出模板"
                    Command="{x:Bind ViewModel.ExportTemplateCommand, Mode=OneWay}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
