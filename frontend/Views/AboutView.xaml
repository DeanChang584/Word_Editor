<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WordFormatterUI.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WordFormatterUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="ViewRoot">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- HyperlinkButton style: normal secondary (matches "联系邮箱"), hover = SystemAccentColor -->
            <Style x:Key="AboutLinkButtonStyle" TargetType="HyperlinkButton">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="HyperlinkButton">
                            <Grid x:Name="Root" Background="{TemplateBinding Background}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemAccentColor}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemAccentColor}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Padding="{TemplateBinding Padding}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <!--
        AboutView: software name, version, GitHub link, copyright (plan §7.7).
    -->
    <controls:ConfigCard Title="关于软件">
        <StackPanel Spacing="12">

            <!-- App name + icon -->
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <FontIcon
                    Glyph="&#xE8A5;"
                    FontFamily="Segoe Fluent Icons"
                    FontSize="36"
                    Foreground="{ThemeResource SystemAccentColor}"
                    VerticalAlignment="Center" />
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Word Formatter" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock
                        x:Name="VersionText"
                        Text="版本 v2.1"
                        FontSize="14"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </StackPanel>

            <!-- Description -->
            <TextBlock
                Text="一款用于 Windows 平台的 Word 文档格式处理工具，支持 .doc 和 .docx 文件。"
                FontSize="14"
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- Framework info -->
            <TextBlock
                Text="开发框架：前端 WinUI 3；后端 Python FastAPI"
                FontSize="14"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- Developer -->
            <TextBlock
                Text="开发人员：Dean Chang (TechWordsInSight)"
                FontSize="14"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- GitHub link -->
            <HyperlinkButton
                x:Name="GitHubLink"
                Content="项目主页：GitHub"
                NavigateUri="https://github.com/DeanChang584/WordFormatter"
                Style="{StaticResource AboutLinkButtonStyle}"
                Padding="0" />

            <!-- Email -->
            <StackPanel Orientation="Horizontal" Spacing="4">
                <TextBlock
                    Text="联系邮箱："
                    FontSize="14"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    VerticalAlignment="Center" />
                <HyperlinkButton
                    Content="zhangyx584@163.com"
                    Click="EmailLink_Click"
                    Style="{StaticResource AboutLinkButtonStyle}"
                    Padding="0"
                    VerticalAlignment="Center" />
            </StackPanel>

            <!-- Copyright -->
            <TextBlock
                Text="© 2026 Word Formatter. 保留所有权利。"
                FontSize="14"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}" />

        </StackPanel>
    </controls:ConfigCard>
</UserControl>
